# Copy to .env and fill in your real GA4 Measurement ID once one exists.
# Without this, the app falls back to a dummy placeholder ("G-XXXXXXXXXX")
# — analytics still won't fire on localhost either way (see src/lib/analytics.ts).
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
